# تست WebSocket‌ها

شما می‌توانید از همان `TestClient` برای تست WebSocket‌ها استفاده کنید.

برای این کار، از `TestClient` در عبارت `with` استفاده کنید و به WebSocket متصل شوید:

{* ../../docs_src/app_testing/tutorial002.py hl[27:31] *}

/// note

برای جزئیات بیشتر، مستندات Starlette را برای <a href="https://www.starlette.io/testclient/#testing-websocket-sessions" class="external-link" target="_blank">تست نشست‌های WebSocket</a> بررسی کنید.

///
