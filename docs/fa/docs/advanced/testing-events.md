# تست رویدادها: startup - shutdown

هنگامی که نیاز دارید هندلرهای رویداد (`startup` و `shutdown`) در تست‌های شما اجرا شوند، می‌توانید از `TestClient` با عبارت `with` استفاده کنید:

{* ../../docs_src/app_testing/tutorial003.py hl[9:12,20:24] *}
