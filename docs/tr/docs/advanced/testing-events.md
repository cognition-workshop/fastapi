# Olayları Test Etme: startup - shutdown

Testlerinizde olay işleyicilerinizin (`startup` ve `shutdown`) çalışmasına ihtiyacınız olduğunda, `TestClient`'ı bir `with` ifadesiyle kullanabilirsiniz:

{* ../../docs_src/app_testing/tutorial003.py hl[9:12,20:24] *}
